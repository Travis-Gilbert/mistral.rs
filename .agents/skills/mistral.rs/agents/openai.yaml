interface:
  display_name: "Mistral.rs"
  short_description: "Repo-specific patterns and workflows for mistral.rs"
  default_prompt: "Use the mistral.rs repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true